# ─── Canton Devnet Environment Template ───
# Copy to devnet.env and fill in real values.
# NEVER commit devnet.env to the repository.

# Canton JSON API endpoint (devnet participant node)
CANTON_JSON_API_URL=http://devnet-participant.canton.network:7575

# Canton gRPC endpoint
CANTON_GRPC_URL=devnet-participant.canton.network:6865

# Canton environment
CANTON_ENV=devnet

# Disable mock mode for real devnet connection
CANTON_MOCK=false

# JWT token for Canton JSON API authentication
CANTON_JWT_TOKEN=your-canton-jwt-token-here

# TLS certificate path (if required)
# CANTON_TLS_CERT_PATH=/path/to/cert.pem

# ─── Party Identifiers ───
# These are assigned by the Canton devnet operator
CANTON_OPERATOR_PARTY=operator::devnet-participant
CANTON_ORACLE_PARTY=oracle::devnet-participant
CANTON_CREDIT_ASSESSOR_PARTY=creditassessor::devnet-participant
CANTON_TREASURY_PARTY=treasury::devnet-participant
